return {
    -- "nvim-tree/nvim-tree.lua",
    -- version = "*",
    -- -- dependencies = { "nvim-tree/nvim-web-devicons" },
    -- config = function()
    --   require("nvim-tree").setup {
    --         renderer = {
    --             icons = {
    --                 show = {
    --                     file = true,     -- отключить иконки файлов
    --                     folder = true,   -- отключить иконки папок
    --                     git = true,      -- отключить иконки Git (если не нужны)
    --
    --                 },
    --             },
    --         },
    --     }
    --     vim.keymap.set('n', '<leader>e', ':NvimTreeToggle<CR>', { silent = true, noremap = true })
    -- end,
}
